using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoordinateLocation : MonoBehaviour
{
    public Vector2 coordinates = new Vector2(-1, -1);

    // Start is called before the first frame update
    void Start()
    {
    }
}
